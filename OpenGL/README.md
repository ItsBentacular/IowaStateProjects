**this is my spring 2026 project BL3M (Name is WIP)**, 
which is a C/C++ OpenGL project that will hopefully end up being a full model viewer with both ray trace and shader support.
Im not sure where it will actually end up feature-wise, but I think graphics programming is very interesting, so I thought I would start experimenting
with it.
